import numpy as np
import random

# Set matrix dimensions
n = 6
m = 5

# Initialise matrix
matrix = np.ones((n, m), dtype=int)

# Initialise start point
start_x_position = random.choice(value) for value in range(n)


start_point = random.choice(matrix)
print(start_point)